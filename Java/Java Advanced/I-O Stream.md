## Byte streams(binary data)
- Work with **raw bytes (8-bit)**
- Base classes:
	- `InputStream`
	- `OutputStream`

## Character streams(text data)
- Work with characters
- Handle encoding
- Used for: text files, human-readable data
- Base classes:
	- Text files
	- Human-readable data
- 